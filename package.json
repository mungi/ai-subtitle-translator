{
  "name": "ai-subtitle-translator",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "find extension -name '*.js' -print0 | xargs -0 -n1 node --check && node -e \"JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8')); console.log('manifest ok')\"",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "terser": "^5.49.0",
    "yazl": "^3.3.1"
  },
  "dependencies": {
    "@openai/codex": "^0.144.3"
  }
}
